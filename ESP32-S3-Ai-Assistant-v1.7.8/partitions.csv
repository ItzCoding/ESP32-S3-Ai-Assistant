# ESP32-S3  16 MB Flash  —  OTA + FFat
# ──────────────────────────────────────────────────────────────────
# Two equal OTA app slots (6 MB each) so httpUpdate can write the
# new firmware while the current one is running, then swap on reboot.
# FFat gets the remaining ~3.5 MB for facts/notes/skills/history.
# ──────────────────────────────────────────────────────────────────
# Name,     Type,  SubType,  Offset,    Size,    Flags
nvs,        data,  nvs,      0x9000,    0x5000,
otadata,    data,  ota,      0xe000,    0x2000,
app0,       app,   ota_0,    0x10000,   0x600000,
app1,       app,   ota_1,    0x610000,  0x600000,
ffat,       data,  fat,      0xC10000,  0x3F0000,
